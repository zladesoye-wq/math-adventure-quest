{
  "name": "math-adventure-quest",
  "version": "1.0.0",
  "description": "A gamified math learning platform for kids ages 5–11.",
  "scripts": {
    "dev:server": "npm run dev --prefix packages/server",
    "dev:client": "npm run dev --prefix packages/client",
    "dev": "npx concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "build:server": "npm run build --prefix packages/server",
    "build:client": "npm run build --prefix packages/client",
    "build": "npm run build:server && npm run build:client",
    "test": "npm run test --prefix packages/server",
    "install:all": "npm install --prefix packages/server && npm install --prefix packages/client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
